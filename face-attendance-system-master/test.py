def test(image, model_dir, device_id):
    """
    Placeholder for the anti-spoofing test function.
    Returns 1 (real face) to allow the attendance system to work without the anti-spoofing models.
    """
    return 1
